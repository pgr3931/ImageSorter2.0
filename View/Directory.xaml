<UserControl x:Class="ImageSorter2._0.View.Directory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ImageSorter2._0.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Button Width="150" Height="150" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Width="75" VerticalAlignment="Center" HorizontalContentAlignment="Center" FontSize="20">📂 F</Label>
            <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="18" VerticalAlignment="Center" TextWrapping="WrapWithOverflow" TextAlignment="Center"/>
        </Grid>
    </Button>
</UserControl>