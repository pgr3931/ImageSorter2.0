<UserControl x:Class="ImageSorter2._0.View.Directory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ImageSorter2._0.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Button Width="130" Height="130" Margin="5"
            Command="{Binding Path=DataContext.MoveCommand, 
          RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
            CommandParameter="{Binding Index}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" VerticalAlignment="Center" HorizontalContentAlignment="Center" FontSize="20"
                   Content="{Binding Shortcut}" />
            <TextBlock Grid.Row="1" Text="{Binding Name}" FontSize="18" VerticalAlignment="Center"
                       TextWrapping="WrapWithOverflow" TextAlignment="Center" />
        </Grid>
    </Button>
</UserControl>